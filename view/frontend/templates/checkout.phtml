<?php
/**
 * Reference checkout template.
 *
 * @var \Magento\Framework\View\Element\Template $block
 * @var \Magento\Framework\Escaper $escaper
 */
?>
<div class="sc-checkout">
    <?= $block->getChildHtml('softcode.formkey') ?>

    <h1><?= $escaper->escapeHtml(__('Checkout')) ?></h1>

    <section class="sc-section">
        <h2><?= $escaper->escapeHtml(__('Buyer type')) ?></h2>
        <label><input type="radio" name="company_type" value="privat" checked> <?= $escaper->escapeHtml(__('Private')) ?></label>
        <label><input type="radio" name="company_type" value="cvr"> <?= $escaper->escapeHtml(__('Company (CVR)')) ?></label>
        <label><input type="radio" name="company_type" value="ean"> <?= $escaper->escapeHtml(__('Public sector (EAN)')) ?></label>

        <div id="sc-company-fields">
            <input type="text" id="sc-company-name" placeholder="<?= $escaper->escapeHtmlAttr(__('Company name')) ?>">
            <input type="text" id="sc-cvr" placeholder="<?= $escaper->escapeHtmlAttr(__('CVR number')) ?>">
            <input type="text" id="sc-ean" placeholder="<?= $escaper->escapeHtmlAttr(__('EAN number')) ?>">
        </div>
    </section>

    <section class="sc-section">
        <h2><?= $escaper->escapeHtml(__('Contact details')) ?></h2>
        <input type="email" id="sc-email" placeholder="<?= $escaper->escapeHtmlAttr(__('Email')) ?>" required>
        <input type="text" id="sc-firstname" placeholder="<?= $escaper->escapeHtmlAttr(__('First name')) ?>">
        <input type="text" id="sc-lastname" placeholder="<?= $escaper->escapeHtmlAttr(__('Last name')) ?>">
        <input type="tel" id="sc-phone" placeholder="<?= $escaper->escapeHtmlAttr(__('Phone')) ?>" required>
    </section>

    <section class="sc-section">
        <h2><?= $escaper->escapeHtml(__('Address')) ?></h2>
        <input type="text" id="sc-street" placeholder="<?= $escaper->escapeHtmlAttr(__('Street')) ?>" required>
        <input type="text" id="sc-housenumber" placeholder="<?= $escaper->escapeHtmlAttr(__('House number')) ?>">
        <input type="text" id="sc-postcode" placeholder="<?= $escaper->escapeHtmlAttr(__('Postcode')) ?>" required>
        <input type="text" id="sc-city" placeholder="<?= $escaper->escapeHtmlAttr(__('City')) ?>" required>
    </section>

    <section class="sc-section">
        <h2><?= $escaper->escapeHtml(__('Payment')) ?></h2>
        <div id="sc-payment-mount"></div>
        <p id="sc-payment-message" class="text-danger" style="display:none;"></p>
    </section>

    <section class="sc-section">
        <h2><?= $escaper->escapeHtml(__('Discount code')) ?></h2>
        <input type="text" id="sc-coupon-code" placeholder="<?= $escaper->escapeHtmlAttr(__('Discount code')) ?>">
        <button type="button" id="sc-apply-coupon"><?= $escaper->escapeHtml(__('Apply')) ?></button>
        <p id="sc-coupon-message" style="display:none;"></p>
    </section>

    <section class="sc-section" id="sc-cart"><!-- cart summary loaded by cart.js --></section>

    <section class="sc-section">
        <label><input type="checkbox" id="sc-accept-terms"> <?= $escaper->escapeHtml(__('I accept the terms and conditions.')) ?></label>
        <p id="sc-order-message" class="text-danger" style="display:none;"></p>
        <button type="button" id="sc-place-order" disabled><?= $escaper->escapeHtml(__('Place order')) ?></button>
    </section>
</div>
